[project]
name = "ghastcoiler"
version = "0.1.0"
description = "Vendored fork: Hearthstone Battlegrounds simulator"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ghastcoiler"]
